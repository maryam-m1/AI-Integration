# Gemini API Integration - Day 3

Focus: Structured JSON Output Generation using Google Gen AI SDK (`@google/genai`).

## Features
- Configured `responseMimeType: "application/json"` to force strict JSON schema responses.
- Evaluated code snippets programmatically and parsed the response into JavaScript objects.